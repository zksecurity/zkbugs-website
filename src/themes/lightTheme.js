import createPalette from "@mui/material/styles/createPalette";

export const lightPalette = createPalette({
  mode: "light",
  background: {
    default: "#ffffff",
    paper: "#f4f4f4",
  },
  borders: {
    default: "#e0e0e0",
  },
});
